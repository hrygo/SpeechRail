# SDD ledger — plan: docs/superpowers/plans/2026-09-25-issue-95-asr-tts-target-architecture-luna-guide.md

Base: cedbb19dd05a1ba5ca43c2744f54ff0cdbc7e1e0
Branch: codex/issue-95-target-architecture
Worktree: /Users/hrygo/.codex/worktrees/issue-95-asr-tts/SpeechRail
Spec: docs/architecture/2026-09-25-asr-tts-target-architecture-no-legacy.md (read; accepted)
Ledger created: 2026-09-25 Asia/Shanghai
Pre-flight: shared interfaces in T01→T02/T10/T11, T02→T04/T05/T07/T08/T09/T10/T11, T03→T04/T05/T07/T12, T04→T05/T06/T07/T09/T11, T05→T06/T10, T06→T10, T07→T08/T09/T11, T09→T10, T10→T11; no unresolved conflicts at plan level. Task briefs will be checked per task.
Pre-flight: the plan declares T12 needs separate authorization and quality thresholds not yet frozen; implementation will stop at that gate and request human acceptance.
Pre-flight: no explicit commit/push authorization in this execution request, so task commits are deferred unless later authorized; this is a deviation from the generic executing-plans commit step and will be reported.
Task 1: complete — added strict `contracts/realtime-events.schema.json`, 40 shared cases under `tests/fixtures/realtime-current/`, field matrix, Python/Swift mechanical checks, current-only contracts/docs. Tests: `pytest tests/test_realtime_current_schema.py` 7 pass; `scripts/check_realtime_contract.py` 40 fixtures/32 fields; targeted Swift fixture test pass; Ruff pass; OpenAPI lint pass. No commit.
Task 2: Ruling: catalog 12 项集合/真实 hash/revision/wheel builder 推迟到 T03；T02 只实现 spec 领域/解析器/selection v2 并映射现有 10 项工件 — 计划 T02/T03 在 catalog 结构上重叠 — cost if wrong: T03 需回改映射表。
Task 2: complete — 新增 `domain/model_spec.py`、`domain/task_plan.py`、`application/plan_resolver.py`；`profile_store` selection v2（旧格式 `LegacySelectionError`，不覆写旧文件）；`config/selection.py` 按显式 artifact key 解析、删除目录名推断；`model_store`/`model_commands`/`profile_commands`/`cli`/`managed_install`/`diarization_assets` 改为双 spec + opt-in 辅助能力。Tests: `tests/test_task_plan.py`、`tests/test_spec_selection.py`、`tests/test_profile_store.py`、`tests/test_profile_selection.py`、`tests/test_model_store.py`、`tests/test_model_commands.py`、`tests/test_profile_switch.py`、`tests/test_profile_commands.py`、`tests/test_installer.py`、`tests/test_cli.py`、`tests/test_cli_install.py`、`tests/test_cli_machine_output.py` 共 247 pass（--no-cov）；`tests/test_model_presets.py tests/test_model_identity.py tests/test_resource_governor.py tests/test_capability_snapshot.py` 145 pass。未改动 `assets/model-catalog.json`；T03 待补 3 个缺失制品。Deferred (not T02 scope, red until later tasks): `tests/test_voice_design_registration.py`（T07/T08 路由改 plan role）与 `tests/test_render_receipt_routes.py::test_v1_accepts_namespaced_model_revision_pin`（T07/T10 revision 身份）仍依赖旧 preset 推断。No commit.
Task 3: Ruling: catalog 12 制品（10−1+3）真实 source revision/hash/size 由 ModelScope API + `git ls-remote` 核实（2026-09-25），写入 `assets/model-catalog.json` 并冻结为发布矩阵；catalog builder 与 `ModelCatalog` 拒绝猜测值。Ruling: engine wheel 取代 vendor overlay —— 固定上游 revision + patch/build 输入 hash 进入 `runtime-lock.json` 的 `engine_wheel`，无 provenance 时不写 pin（fail-closed），不伪造 hash。Ruling（用户 2026-09-25）: 高精度制品（custom q8/bf16）继承对应低精度门禁证据，不逐项复测；数值阈值未冻结，真实设备/播放器/长稳认证仍属 T12 专项授权。Ruling（用户授权 2026-09-25）: 缺失制品准备授权已给，按 `prepare_spec_models` + `models/<artifact_key>` 布局异步下载，不阻塞任务、不写入仓库；下载进程在 worktree 之外运行。
Task 3: complete — `assets/model-catalog.json` 冻结 12 制品/13 specs（删除 `tts-1.7b-design-q8`，新增 `tts-0.6b-base-q8`、`tts-1.7b-custom-q8`、`tts-1.7b-custom-bf16`），`load_catalog()` 调 `assert_target_spec_bindings()` 强制发布矩阵精确匹配；`model_store` 取 asr/tts_custom_voice/tts_base 三制品并按 `models/<artifact_key>` 异步准备。受控 engine wheel：`config/model_catalog.py::EngineWheelPin`、`RuntimeLock.engine_wheel`、`service/bootstrap.py`（`uv pip install --no-deps --only-binary :all:` + `engine-wheel/manifest.json` 校验）、`tools/update_runtime_lock.py`（读 `vendor/engine-build/dist/provenance.json`）、新增 `tools/build_engine_wheel.py`、`hatch_build.py` 打包 wheel 或 fail-closed；删除 `vendor_overlays`/`runtime_overlay_source`。Tests: 组合 pytest（`test_model_presets`/`test_model_catalog_builder`/`test_model_store`/`test_profile_*`/`test_installer`/`test_capability_snapshot`/`test_runtime_*`/`test_engine_wheel_build`/`test_hatch_build`/`test_model_commands`）全绿；`ruff check src tests tools hatch_build.py` All checks passed。未跑真实 engine wheel 构建（需固定上游源码+网络）：停在证据门，不伪造 hash。真实制品准备异步进行中（tts-0.6b-base-q8 已完成；tts-1.7b-custom-q8/bf16 进行中）。No commit.
Task 4: Ruling: 同一模型的多个音色/请求不得复制权重 —— owner 由 `ModelOwnerKey(artifact_key, artifact_revision, engine_revision, compute_config)` 唯一确定，voice/request/task 身份只改变 lease 内的参考条件与 utterance 状态；两声音共享 Base owner。Ruling: 已 drain 的 owner 保持 `DRAINING` 终止态、拒绝新 lease（不静默复活旧权重）；`stop()` 才允许重新握手。
Task 4: partial — 新增 `runtime/model_owner.py`：`OwnerState`（unloaded/loading/ready/busy/draining/failed）、`ModelOwner`（single-flight load、引用计数、identity handshake、cancel/error/timeout 释放、begin_drain 等待活动 lease 后卸载、stop 可复用）、`ModelOwnerRegistry`（register/owner/acquire/by_artifact/states/drain_artifact/stop_all）、`ModelOwnerKey.from_artifact()`（从 catalog artifact identity 派生）。新增 `tests/test_model_owner.py` 13 pass（并发获取只 load 一次且共享实例；refcount；identity 不符 fail-closed 且可重试；drain 中拒绝新 lease；cancel/error/timeout 各释放一次；stop 后重新握手；registry 按 compute_config 隔离、按 artifact 分组 drain）。ruff 通过。现状核实：`Qwen3TtsCapabilityRouter` 已按能力（primary+clone）复用 worker 而非按 voice 建副本，故“重复请求不复制模型”在 router 层已成立。剩余（T04 未完成）：把 owner registry 接入 `application/services.py`/router、governor 与 capability snapshot（含 engine/voice/selection generation 使 ETag 失效、非认证组合不报全双工），并更新 `tests/test_resource_governor.py`/`test_capability_snapshot.py` 断言；已跑相关组合 86 pass 无回归。No commit.
Task 3: 制品准备完成（异步、worktree 外）—— fast/fast、quality/quality、reference/reference 三次 prepare 全部 `verified`，prepared_id `prepared_f20cbe267d92f66d32614d04a9f0157a23de7d73487efaeb8ec4e996f8529adb`；对照 `assets/model-catalog.json` 校验 10 个模型制品逐文件 size 全匹配（asr×3、tts base/custom/design×7）；aligner-q8/bf16 属 opt-in 辅助资产（由 diarization/profile apply 单独准备），不在三档 prepare 内。证据类型：本机实测（2026-09-25 20:31 Asia/Shanghai）；未做质量/性能/长稳验证。No commit.
Commit 记录（用户 2026-09-25 授权「及时 commit」）：`7d8c9bf5` T01 Realtime wire 锁定；`d42271ca` T02+T03 ModelSpec/ResolvedPlan + 12 制品 + 受控引擎；`43b97288` T04 owner 与预算 schema；`41422b8a` 账本；`4230112e` 能力快照 ASR 操作与全双工保证；`b0a01d32` 缺资源画像时拒绝设置组合的重计算。T02/T03 合并提交：两者对 model_store/cli/managed_install 等文件互相依赖，单独提交会产生不可运行中间态，故按一个逻辑主题落地。
Task 4: complete — 逐项对照：①owner key=artifact+engine+compute，voice/request 不建副本（`Qwen3TtsCapabilityRouter` 已按能力复用 worker）；②状态机与引用计数、identity 握手、cancel/error/timeout 恰好释放一次、drain 终态（`tests/test_model_owner.py` 13 项）；③`ComponentFootprint` 显式拆分 resident/共享依赖/活动峰值/工作空间/安全余量，`includes_shared_dependencies` 防止同一峰值重复计账（`tests/test_model_budget.py`）；④由 settings 组合且启用但未声明峰值时拒绝重计算，声明峰值装不下时 `GovernorBudgetError` 拒绝，注入组件由注入方负责（`tests/test_application_composition.py`、`tests/test_resource_governor.py`）。Ruling: 资源画像由 `SPEECHRAIL_*_RESIDENT_BYTES` 声明（本机已声明 ASR/TTS/Diarization 三项），故本机行为不变；未声明的新部署将显式拒绝重计算而非按旧默认放行。
Task 4: complete（能力快照 #82）— `operations` 增补 `transcription`/`alignment_transcription`/`realtime_transcription`/`jobs`：真实输入上限（`max_upload_bytes`/`max_audio_seconds`）、粒度、语言取值域、输出能力与就绪原因；`guarantees.realtime_full_duplex` 仅在联合实时验收后为 true，未认证报 `half_duplex`；engine revision、selection generation、认证事实纳入 `catalog_revision` 以保证 ETag 失效；快照不接受 busy 输入，支持能力枚举不因繁忙变形。Tests: `tests/test_capability_snapshot.py` 新增 5 项。
证据（重要的既有红灯，必须记录）：在 commit `4230112e` 上实测，`tests/test_speech_api.py`、`test_tts_voices_api.py`、`test_tts_voice_clone.py`、`test_voice_design_registration.py`、`test_voice_quality_routes.py`、`test_render_receipt_routes.py` 共 48 项失败（26/11/4/4/2/1），均为 T02/T03 后遗留、按计划归属 T07/T08/T10/T12；另有 `tests/test_tts_stream_application.py` 与 `tests/test_realtime_tts_incremental.py` 在本机稳定挂起（baseline 与当前同样 11 项后停滞），不得据此断言通过。T04 的实现与测试对这些红灯中性：改动前后失败集合完全一致（48=48）。
Task 5: complete — ASR 流式策略已收敛为 `chunk_duration_ms` 500/1000/2000 + 引擎真实 `max_context_sec`；Q8/BF16 加载身份与 engine revision 显式回读，禁止 BF16 临时量化冒充 Q8。Realtime text final 与 Alignment 分离：completed 先发且不再等待 aligner，alignment.done/failed 独立异步发送；hypothesis revision/stable prefix/final 共用同一 partial 事实序列，不二次 ASR。commit/endpoint 增加单一 owner 幂等保护，重复提交不产生双 final，空文本仍有确定 final。#83：新增 `speechrail_realtime_first_hypothesis_total{outcome}` 与分阶段 `..._seconds{stage}`、`..._audio_seconds`，区分 admitted/upstream/worker/socket 原点，无 partial 直接 final 记 `missing`，慢发送记 `send_failed`，每 turn 最多一次；定义写入 `docs/operations/security-observability.md`。Evidence: `tests/test_qwen3_worker.py`、`test_qwen3_worker_isolation.py`、`test_qwen3_streaming.py`、`test_asr_mode.py`、`test_realtime_current_schema.py`、`test_realtime_caller_wire.py`、`test_alignment_admission.py`、`test_diarization_alignment.py`、`test_observability.py` 共通过；`scripts/check_realtime_contract.py` 40 fixtures/32 fields；`ruff check src tests tools hatch_build.py` 通过（2026-09-25）。`tests/test_realtime_openai.py` 全量只剩 5 个 TTS 档位/音色 revision 红灯，按计划归属 T07/T10；T05 新增/改写的 ASR 用例全部通过。
Task 6: complete — 采样时间轴与独立 Alignment/Diarization 已按目标架构落地：
(1) `domain/audio_timeline.py`：`SampleSpan` half-open 采样区间、`RateMap`（source/target rate + origin + resampler delay 的有理数映射）、`SampleClock`（按 wire rate 计样本）、`RationalResampler`（整数 phase、有界 pending、reset 清状态）。
(2) 全局 `Span`→`SampleSpan` 更名，唯一定义在 timeline；`domain/diarization/types.py` 只 import。
(3) Alignment 职责迁出 ASR：删除 `application/diarization/alignment.py`（无 alias），新增 `domain/alignment.py`（`AlignmentGranularity`/`AlignmentUnit`/`AlignmentRequest`/`AlignmentResult`/`AlignTextPort`，identity=task/epoch/utterance/transcript_revision）、`application/alignment.py`（`FixedTextAligner` + `validate_alignment`，codepoint `[start,end)`、gap 附件、粒度证据校验、失败绝不返回空 units 成功）、`backends/qwen3_alignment_worker.py`（独立 mlx forced-aligner 进程）、`backends/qwen3_alignment.py`（`Qwen3AlignmentConfig`/`Qwen3AlignmentWorker`，Semaphore 有界队列，close/alive/last_active）。`qwen3_worker.py` 删除 aligner 实例化、`align_text` frame、`_align_buffers`；`qwen3_native.py`/`qwen3_streaming.py` 去掉 `aligner_model_dir`/`--aligner-model-dir`/`capture_alignment`。`application/services.py` 由独立 `Qwen3AlignmentWorker` 构图并纳入 evictable。
(4) 音频保留统一且有界：新增 `runtime/limits.py`（`MAX_PCM_BYTES=40MiB` 全局总量语义 + `MAX_ALIGNMENT_PCM_BYTES`）与 `runtime/pcm_buffer.py`（`BoundedPcmBuffer`/`PcmBufferOverflowError`，pin=不可变快照，超限显式抛错不静默丢音）；`realtime_openai.py` 用 `BoundedPcmBuffer` 承载每 item 的 alignment PCM，commit/failure/cancel 释放，溢出以 `alignment_pcm_overflow` 显式失败。PCM 不落盘。
(5) Realtime：`_alignment_granularity()`（character>word>segment）、`_build_alignment_units` 传全额身份、`_finish_alignment` stale-guard（task/epoch/item/transcript_revision 不符则丢弃为 `fixed_text_stale`）、`AttributionUnit.granularity` 透出。
(6) 契约同步：`contracts/realtime-events.schema.json` 新增 `$defs.alignment_unit`（required `granularity`，codepoint 语义）并收紧 `server_alignment_done.units`；新增 invalid fixture `alignment-unit-missing-granularity`；`server/alignment-done.json` 改为双 unit 真实结构。`contracts/diarization/v1/`：session 契约改为运行时实际回显的 `{enabled,version,max_speakers:4}`，completed 改为文本 final（`status=pending|not_applicable`、`attribution_units` 恒空、对齐单元由 `speechrail.alignment.done` 承载），update 描述改为引用 alignment.done；fixtures 换为 `valid_completed_zh_pending`/`valid_completed_empty_not_applicable`。`contracts/realtime-openai.md` §5.1/§5.2 补 pending 说明、alignment.done 结构与 #85 粒度契约（无粒度证据报 unsupported，不把 phrase 均分伪装 character）。
Evidence（2026-09-25 Asia/Shanghai）：`uv run --extra dev ruff check src tests tools hatch_build.py` All checks passed；`scripts/check_realtime_contract.py` 41 fixtures/32 fields；组合 pytest `tests/test_qwen3_worker.py`/`_isolation`/`_limits`/`test_qwen3_backend.py`/`test_qwen3_streaming.py`/`test_diarization_alignment.py`/`test_alignment_worker.py`/`test_alignment_admission.py`/`test_diarization_attribution.py`/`test_diarization_session.py`/`test_diarization_boundaries.py`/`test_diarization_metrics.py`/`test_openai_diarized_batch.py`/`test_diarization_extensions.py`/`test_audio_timeline.py`/`test_pcm_buffer.py`/`test_realtime_current_schema.py`/`test_asr_mode.py`/`test_realtime_openai.py` 共 364 passed，仅 `test_realtime_openai.py` 5 项 TTS 档位/音色 revision 红灯按计划归属 T07/T10。未跑真实 aligner 进程/真实音频/长稳；`tests/test_tts_stream_application.py`、`test_realtime_tts_incremental.py` 本机稳定挂起（baseline 同），不计入。
T06 后复核（2026-09-25）：对 T02/T03 遗留红灯集合 `tests/test_speech_api.py`/`test_tts_voices_api.py`/`test_tts_voice_clone.py`/`test_voice_design_registration.py`/`test_voice_quality_routes.py`/`test_render_receipt_routes.py` 重跑，仍为 48 项失败（与 commit `4230112e` 记录一致），T06 对这批红灯中性。TTS 档位/音色 revision 与 Design 发布按计划归属 T07/T08/T10。
Task 7: complete — TTS 路由收敛为 plan role：`tts_routing.py` 将 clone 映射为 `tts_base`、system 映射为 `tts_custom_voice`，VoiceDesign instruction 明确拒绝进入普通 synthesize/open_stream；router 改为按 role 持有 worker，缺 role 不回退其他精度或角色。voice binding、reference condition、validation binding、capability key、render receipt plan/window/checkpoint 身份均绑定 `{tier}.{render|stream}`，同一 voice ID 跨 engine/precision 不复用缓存或证据；REST/Realtime/models/voices 的 broker、artifact 和 capability 均由同一 role 解析。Evidence（2026-09-25 Asia/Shanghai）：`tests/test_qwen3_tts_capability_router.py`、`_availability.py`、`_lifecycle.py`、`test_tts_reference_condition.py`、`test_voice_bindings.py`、`test_tts_role_routing.py`、`test_voice_validation.py`、`test_render_receipts.py`、`test_application_composition.py`、`test_capability_snapshot.py`、`test_tts_stream_capability_matrix.py`、`test_realtime_openai.py`、`test_render_receipt_routes.py`、`test_speech_api.py`、`test_tts_voices_api.py`、`test_tts_voice_clone.py` 共 389 passed；`ruff check src tests tools hatch_build.py` All checks passed。未跑真实模型、音频、服务运行态、性能或长稳。
T07 后边界复核：`tests/test_voice_design_registration.py` 的 26 项失败属于 T08 尚未实现的“候选生成→Base 新文本复验→原子发布”流程，不是 T07 role 路由回归；`tests/test_voice_quality_routes.py` 的 11 项失败属于 T08/T10 仍使用旧 selection fixture 与旧 validation identity 的迁移范围。此前已知 `tests/test_tts_stream_application.py`、`test_realtime_tts_incremental.py` 本机稳定挂起（baseline 同），均未计入 T07 通过。
Task 8: complete — `/v1/voice-designs` 实现候选生命周期 `generated → confirmed → validating → publishable → published`（cancelled/failed 终态）。候选 JSON/WAV 原子落盘于 registry sibling 私有目录，生产 `/v1/voices` 不显示候选；Base 复验必须使用与参考不同的受控/显式测试文本，机器证据与人工 identity/naturalness 听审分离；validate 编辑参考文本会生成新 revision 并清旧证据；publish 在 registry 锁内复核 revision 并 create-only 注册，重复 publish 返回既有 voice。MCP 改为 `design_voice → confirm_voice_design → validate_voice_design → publish_voice_design`，旧 `/v1/voices/designs` 直接发布语义移除。同步 OpenAPI、用户/架构/MCP/Skill 文档。
Task 8 evidence（2026-09-26 Asia/Shanghai）：`tests/test_voice_design_workflow.py` 15 pass；`tests/test_voice_design_registration.py` 31 pass；`tests/test_voice_quality_routes.py` 37 pass（fixture 迁移到 selection v2/required artifact）；组合 `tests/mcp/test_server.py tests/mcp/test_tools.py tests/mcp/test_resources.py` 通过；组合 T07/T08 定向集共 230 pass；`ruff check src tests tools hatch_build.py` All checks passed；OpenAPI YAML parse 通过。未跑真实 VoiceDesign/Base/ASR、真实音频、性能、长稳或 UI；App `ServiceAPIClient` 仍指向旧路径，按计划归 T10/T11 同批切换，未在本提交部署。
Commit: `e53bf11` (`feat: 分离 VoiceDesign 候选复验与发布`)。
Task 9: complete — ①状态机/owner 保留，plan+voice revision 整轮固定；追加不重新 prepare reference、不重建 utterance：真实服务 bench 断言 `synthesizer.open_calls == 1`、append N、finish 1（`tests/test_bench_tts_streaming.py::test_run_incremental_turn_drives_the_real_realtime_server`）。②客户端 ACK 精确匹配 request/sequence，音频/状态包不能释放文字窗口：新增 `test_audio_or_status_does_not_release_an_append_waiter`、`test_run_incremental_turn_waits_for_the_matching_ack_not_any_packet`、`test_run_incremental_turn_rejects_a_mismatched_ack_sequence`；finish 以最后 accepted sequence 为屏障，`waiting_for_text` 不触发完成（`test_finish_requires_the_exact_last_accepted_sequence_and_closes_once`、`test_starved_input_ends_the_utterance_at_the_input_deadline`）。③cancel/failure terminal 不被本请求排队 PCM 阻塞：`StreamPump` 改为 `deque`+`Condition`，`submit_terminal` 立即以 `on_sent` 归还本 request 的排队 PCM 并保持提交顺序（terminal 仍在自己的 `started`/`text_accepted` 之后、下一轮帧之前，不抢占控制帧），pump 继续服务下一轮 utterance；已结束 request 有界记忆（`_known_stream_ids` `deque(maxlen=8)`）与未知外来 id fail-closed 保留。④harness 拆 sender/receiver：sender 等“匹配序号的 ACK”而非任意事件，接收预先 outstanding，`--append-interval-ms` sleep 不再阻塞收音（`test_interval_sleep_does_not_block_audio_receive`）。⑤`recv` 由 daemon reader 承载，外层单调 deadline，超时主动中断传输并有限等待，不遗留 reader 抢占下一轮（`test_receive_interrupts_a_transport_that_never_returns_an_event`）。⑥按 §6.4 更正测量：first PCM 锚定首个稳定文本发送、另报 start→started；`generation_rtf` 不再扣 gap（gap 单独报 `text_gap_ms`）；headroom 在加入新包前判断（可显负值）；成功分布只含 `terminal=completed` 且 PCM 有效，出音后失败/超时计失败、cancelled 单独归类；新增 `first_pcm_before_finish_turns`，lifecycle 报告分开 `terminal_turns`/`release_proven_turns`。⑦旧报告标注：流式报告 schema 提升为 `speechrail-perf/tts-streaming/2`（锚点/分母已变，不再与 `/1` 可比），并在 `.agents/skills/speechrail-perf-benchmark/references/reporting.md` 记录“`/1` 摘要为旧口径，原样保留、不继承认证”；仓库内不存在旧流式报告文件（原始报告按规则留在仓库外），故不改写任何历史数字。
Task 9 evidence（2026-09-26 Asia/Shanghai）：组合 `tests/test_tts_stream_state.py`、`test_tts_stream_application.py`、`test_tts_stream_worker.py`、`test_tts_stream_client.py`、`test_tts_incremental_vendor_state.py`、`test_bench_tts_streaming.py`、`test_bench_tts_stream_lifecycle.py`、`test_qwen3_tts_worker.py` 共 **135 passed**（`--no-cov`）；`ruff check src tests tools hatch_build.py examples` All checks passed。附带修红：`tests/test_tts_stream_application.py` 的 `_FakeSynthesizer` 仍返回 T07 已拒绝的 `lane:<voice>` 形状，导致该文件 11 项全部 `invalid_tts_resource_key` 失败；改为返回声明角色 lane（`tts_custom_voice`/`tts_base`）后 11 项全绿——此前账本记为“本机稳定挂起”，本次实测为稳定失败而非挂起。未跑真实模型、真实音频、服务运行态、真实性能基准、长稳或 UI 自动化。
Task 10: complete — Realtime 协议切成单栈（无旧事件、无 alias、无双写过渡），Python/契约、Swift/App、MCP/示例、正式文档同批落地：
(1) Python/契约：`application/realtime_openai.py` 重写为会话式口径（`session.update` 带 `session.type=transcription`、`session.audio.input.format={type:audio/pcm,rate:24000}`、`session.audio.input.transcription.model`、`turn_detection` null|"manual"、`session.speechrail.{task,tts,alignment,diarization,endpointing,expected_asr_revision,expected_tts_revision}`），服务端只回 `session.created/updated`；`input_audio_buffer.append/commit/clear` 无服务端 ack，关闭屏障=声明 item 数 vs 转写终态数；ASR 用 `speechrail.transcription.hypothesis`（`utterance_id`/`revision`/`text` 最新全文，替换不追加）+ `conversation.item.input_audio_transcription.completed`（仅文本）+ 独立 `speechrail.alignment.done|failed`、`speechrail.diarization.updated|done|failed`；TTS 用 `speechrail.tts.start|append_text|finish_text|cancel` 与 `speechrail.tts.audio.delta`（顶层 `task_id`/`request_id`/`chunk_index`/`sample_offset`/`delta`）、终态 `completed|cancelled|failed`。删除并只保留在拒绝矩阵：`transcription_session.update|updated`、`speechrail.tts.create`、所有 `response.*`、扁平 `input_audio_format`、`speech_capabilities`、`input_audio_buffer.committed|cleared|speech_started|stopped`、diarization `status`/`speaker_links`/`finalize`。契约/文档同步 `contracts/realtime-openai.md`、`contracts/realtime-events.schema.json`、`contracts/diarization/v1/`（旧 v1 schema 与 fixtures 删除）、`docs/users/api-contract.md`、Skill 参考与用户/架构/运维/QoS 文档、`tests/realtime_wire.py`（新共享 wire 断言模块）与 13 个 `tests/*.py`。
(2) Swift/App 客户端状态机重写：`RealtimeContractTypes.swift`、`RealtimeASRClient.swift`、`SessionCoordinator.swift`、`SessionStore.swift`、`CaptionSession/MeetingSession/AssistantSession/TeleprompterSession/TeleprompterFollowController/SpeakerLabeling/AssistantTTSStreamCoordinator` 全部改为新会话口径与顶层 TTS 音频字段；3 处 App 目标的元组模式不匹配（`.ttsTextAccepted` 需 4 元 `(requestID:taskID:appendSequence:totalCodepoints:)`）在 `CaptionSession.swift`、`MeetingSession.swift`、`AssistantSession.swift` 修正。
(3) 采集侧 24 kHz 统一（本次实测发现的真实缺陷）：App 仍以 16 kHz 归一化采集，却在新 wire 声明 24 kHz，服务端会把 16 kHz 字节按 24 kHz 重采样（音高/时长错误）。统一为 24 kHz：`MicrophoneCapture.swift`（`sampleRate` 16_000→24_000）、`CoreAudioTapCapture.swift`、`AssistantAudioSession.swift`（`captureSampleRate`→24_000）、`AudioSourceCoordinator.swift`（`StreamMixer` 用 `MicrophoneCapture.sampleRate`）、`CaptureHelperProtocol.swift` 注释/错误文案；并在 `RealtimeContractTests.swift` 新增 `testCaptureUplinkAndPlaybackShareTheSingleWireSampleRate`。
(4) 文档/示例扫描与归类：确定性使用点全部改到新事件；`contracts/realtime-openai.md`+`docs/users/api-contract.md`+`tests/test_realtime_tts_incremental.py`+`compatibility/openai_realtime.py` 中的旧事件名属有意的拒绝矩阵；`docs/archive/*`、`docs/superpowers/*`、`docs/design/2026-09-18-session-layer*`、`docs/decisions/*`、`docs/implementation/*`、`docs/openai-conformance-audit.md`、`docs/asr-tts-best-practices-and-optimization-spec.md`、`docs/2026-09-08-single-machine*`、`docs/speaker-diarization-e2e-design.md` 属历史/非现行承诺，原样保留；`examples/perf/bench_tts_streaming.py` docstring 与 `realtime/turn_collection.py` 的旧事件名仅作“旧事件已不存在”的说明，保留。
Ruling（ManualTurnCollector，用户 2026-09-26）：`realtime/turn_collection.py` 作为有界参考消费者保留，仅测试用、不进生产路径；它只证明协议关闭屏障（调用方 `expect_item()` 计数），不证明 ASR 质量或逐样本精确验收。
Ruling（继承精度门禁，用户 2026-09-26）：高精度制品继承对应低精度制品的门禁证据，不逐项复测；数值阈值、真实设备、播放器与长稳认证仍属 T12 专项授权，不在 T10 内宣称。
Task 10 evidence（2026-09-26 Asia/Shanghai）：`uv run --extra dev ruff check src tests tools hatch_build.py examples` All checks passed；`uv run --extra dev python scripts/check_realtime_contract.py` 41 fixtures/32 tracked fields；定向 pytest 组合 `test_realtime_openai.py`(86)/`test_realtime_caller_wire.py`(15)/`test_realtime_tts_incremental.py`(15)/`test_realtime_admission_commits.py`(3)/`test_realtime_vad_bargein.py`(6)/`test_diarization_extensions.py`(14)/`test_manual_turn_collection.py`(18)/`test_neural_vad.py`(15)/`test_tts_stream_capability_matrix.py`(12)/`test_bench_tts_streaming.py`(31)/`test_bench_tts_stream_lifecycle.py`(6)/`test_benchmark_scenarios.py`(9)/`test_teleprompter_latency_probe.py`(3)/`test_realtime_current_schema.py`(7) 共 240 passed；`rtk swift test --package-path macos/SpeechRailApp` 215 passed / 0 failed；`rtk scripts/macos_app_build.sh` BUILD SUCCEEDED（Xcode App 目标，临时 derived data，退出即清理/注销）；`scripts/check_operator_docs.py` ok；`scripts/check_version_consistency.py` ok（3.2.1）；`git diff --check` 与 `git diff --cached --check` 通过。
Task 10 finding（方法学，必须记录）：`swift test` 只编译 SwiftPM 库、**不编译 Xcode App 目标**，故 3 处 App 目标的元组模式错误在 `swift test` 下不可见；T10/T11 的 Swift 验证必须包含包装脚本 App 构建。
Task 10 更正（2026-09-26）：此前把 `uv run mypy src` 的 40 项误记为“历史错误”。实测 base `cedbb19dd05a1ba5ca43c2744f54ff0cdbc7e1e0` 为 0 项，当前分支为 40 项，属 T02–T11 引入的静态门禁回归；已在收口提交修复。
Task 10 观察到的 flake：`tests/test_realtime_openai.py::test_realtime_buffer_overflow_auto_commit_rollover` 在并行 CPU 负载下失败过一次（`len(factory.sessions)==1` 断言），单独跑与两次整文件跑（86 passed）均通过；属时序敏感、非 T10 回归，测试未修改。
Task 10 not_run（未验证）：真实模型/真实音频/真实 worker smoke、服务运行态与 profile 切换、T12 的延迟/质量/长稳/播放器验收、`scripts/macos_app_test.sh` Xcode 测试计划（含 UI 测试，需用户逐次明确授权）、真实 engine wheel 构建。

Task 11: complete（用户 2026-09-26 授权「及时 commit」）— 三档独立选择、停播屏障与制作身份固定同批落地：
(1) 三档 + 双 spec：Swift `SpeechRailProfile` 收敛为 `fast/quality/reference`，wire 字段 `asr_spec`/`tts_spec`（`SpecSelection`）；`ControlRequest`/`ProfileSnapshot`/`OperationSnapshot`/控制 Agent/CLI 参数全部迁移。`ProfilePickerView` 快捷三档 + `DisclosureGroup("分别调整识别与配音")` 渐进披露。分人门禁改为「服务能力 + 任务 opt-in」：删除 `SessionPreferences.diarizationGateNote`、`captionSession.diarizationGate` 与 `MeetingSession`/`SettingsAssistantPane` 的按档位拦截。`contracts/openapi.yaml` 的 `/health` 与 `/v1/models` `profile` 由旧四档 enum（`extreme/quality/balanced/light`）改为 `<asr_spec>/<tts_spec>` pattern，示例改 `quality/quality`。
(2) 停播屏障与 epoch 隔离：`AssistantTTSStreamCoordinator.cancel()` 顺序 = invalidate epoch → `await stopPlayback()` → bounded cancelServer → report cancelled；播放回调携带入队 epoch，旧 epoch 晚到回调不得改新账本；新增 cancellation timeout，后端取消超时不恢复旧音。
(3) 制作固定 plan/voice revision：`CreativeWork` 新增 `voiceRevision`/`planID`/`renderRevision`（自定义 decoder 无损读旧 `works.json`）；`CreativeWorkStore.nextRenderRevision` 只追加、不改旧作品；`RenderReceipt` 支持 `plan`，`ServiceAPIClient.createSpeechRender` 用 `receiptMode: "integrity"` 拉 receipt 身份。
(4) 模型页布局与文案同步三档：`Layout.modelProfileCardsFourColumnBreakpoint` → `modelProfileCardsRowBreakpoint`（3×220 + 2×12 = 684pt），`ModelManagementView` 去掉 `profiles.count == 4` 死分支；`ProfilePickerView` 混合选择不再拿单个 TTS 档冒充组合下载量，改为「组合下载量将在准备时按缺失制品校验」。
(5) 文档同步：`DeveloperDocsContent` 分档主题改三档并把 `/v1/voices/designs` 更正为当前 `/v1/voice-designs`；`docs/developers/macos-app-design-system.md` 同步三档/单行断点/档位门禁措辞，版本 0.8.21→0.8.22、date 2026-09-26。
Task 11: 附带修红（T02/T03 遗留，非 T11 引入）—— `config/selection.py::resolve_selection` 把 VoiceDesign 快照当必需目录，`reference` 选择在缺该制品时无法解析；但 prepare（`model_store._selection_artifacts`）从不下载它，且 T02 编写的规格 `tests/test_spec_selection.py::test_every_target_spec_resolves_to_catalog_bound_artifacts[reference]` 只准备 asr/custom/base 目录（此前为红灯，实测 `resolve_selection` 抛 `TTS design model snapshot directory is missing`）。改为与 aligner 同构的 opt-in 语义：新增 `_optional_directory`，设计快照缺失时 `voice_design_artifact_key=None`、能力降级，选择仍可激活。影响：`/v1/models` 与能力快照只在设计快照就绪时声明 VoiceDesign；`list_profiles.download_bytes` 仍按「该档绑定的角色矩阵」计（含 aligner 与 design），与 prepare 集合不必相等（aligner 本来就是单独 provision 的 opt-in 资产）。
Task 11: Ruling（Python drain / 原子切档）: 采用现有 `profile_switch` 口径 —— stop → 单例端口锁释放 → start → smoke → commit，`ProfileStore` 已有 selection v2、generation CAS 与 `PREPARING→VERIFIED→STOPPING→SWITCHING→STARTING→SMOKING→COMMITTED` 事务 journal（失败单次有界回滚）。实际 drain 边界是「旧 ASGI 进程退出并释放单例锁」（`service/lifecycle.py::ServiceLifecycle.stop` 先 disable LaunchAgent、等锁释放，卡住时只对已验证 owner PID 强制停止；start 同样等旧锁释放），比 live owner registry 更粗但更强。不在本任务把未接入 `application/services.py` 的 `runtime/model_owner.py` 冒充运行时热切 —— 代价：切档期间服务短暂不可用，不支持热切换。
Task 11: Ruling（遗留 catalog presets）: `assets/model-catalog.json` 仍保留 legacy `presets`/`precision_policy` 与 `ModelCatalog.preset()`，当前仅 `model_store` legacy `prepare_models(preset_id)` 分支与既有 fixture（`tests/test_capability_snapshot.py`、`tests/test_pronunciation_routes.py` 等）使用；三档选择路径不读它。本轮不顺带删除（删除会牵动 catalog builder/`ModelCatalog` 校验器/多个 fixture 的重构），记为遗留清理项交 T12/后续。
Task 11 evidence（2026-09-26 Asia/Shanghai）：`uv run --extra dev ruff check src tests tools hatch_build.py examples` All checks passed；定向 pytest 组合 `test_spec_selection`+`test_profile_selection`+`test_profile_switch`+`test_profile_store`+`test_profile_commands`+`test_capability_snapshot`+`test_voice_design_registration`+`test_voice_design_workflow`+`test_voice_quality_routes`+`test_tts_voices_api`+`test_realtime_tts_incremental` 共 **236 passed**（`--no-cov`）；`test_profile_switch`+`test_profile_store`+`test_profile_commands`+`test_cli`+`test_installer`+`test_model_store` 组合全绿；`npx --yes @redocly/cli@2.52.1 lint contracts/openapi.yaml` valid；`scripts/check_operator_docs.py` ok；`scripts/check_version_consistency.py` ok（3.2.1）；`scripts/check_realtime_contract.py` 41 fixtures/32 fields；`swift test --package-path macos/SpeechRailApp` 0 failures（swift-testing 144 tests/15 suites + XCTest 全部 suite passed，含 `CreativeWorkStoreTests`、`AssistantTTSStreamCoordinatorTests`）；`scripts/macos_app_build.sh` **BUILD SUCCEEDED**（Xcode App 目标，临时 derived data，退出即清理/注销）。
Task 11 evidence boundary：`swift test` 只编译 SwiftPM 目标，`ProfilePickerView.swift`/`ModelManagementView.swift`/`CaptionSession.swift`/`DeveloperDocsContent.swift` 都在 `SpeechRailAppSupport` 的 exclude 列表里，**不被 swift test 编译**；三档选择页、分人门禁删除、模型页断点与 App 内开发者文档的编译验证只来自 `scripts/macos_app_build.sh`。
Task 11 not_run（未验证）：真实模型/真实音频/真实 worker smoke、服务运行态切档与回滚、真机停播 barrier 听感、T12 延迟/质量/长稳/播放器验收、`scripts/macos_app_test.sh` Xcode 测试计划（含 UI 测试，需用户逐次明确授权）、真实 engine wheel 构建。

Task 12（P5 交付认证）: 停在授权门 — 按计划「质量数值门槛、真实制品与 wheel 认证尚未闭合；P5 运行前须先提出语料分层、盲听规则与各项阈值并经评审确认」，本轮先产出入口条件而非执行认证：
- 新增 `docs/developers/issue-95-certification.md`（`status: draft`）：测量身份字段、ASR/TTS/Alignment/Diarization 语料分层与最小样本建议、组合与设备矩阵、延迟目标（沿用计划 §8.3 口径）、质量与长稳/播放器**建议**阈值（显式标注「评审确认前不是通过标准」）、盲听规则、证据登记表与 `not_run` 清单。
- 计划要求的其余 T12 产物（`docs/users/effective-capabilities.md`、`docs/developers/testing-acceptance.md` 旧四档段落、`docs/operations/README.md`、用户 API/MCP 文档的能力认证集合）**未改**：这一步要与真实证据绑定后一次性更新，避免先写「已认证」再补证据。
- 权威状态核对：`docs/developers/testing-acceptance.md` 仍以 `light/balanced/quality/extreme` 描述的档位与分人供给矩阵**已过期**（T02–T11 已收敛为 `fast/quality/reference` + 独立双 spec）；该文档的更新明确属于 T12，已记入待办，不在本轮顺带改。
- 待用户确认（决策点，见该文 §9）：①是否启动 T12 专项授权；②质量阈值与盲听维度是否采用建议稿；③语料分层/样本量是否够用；④是否允许 UI 自动化及其时间窗。
- 未授权前 not_run（与 T11 相同并扩大）：部署/安装/发布/切档/回滚、真实模型与音频、真实 worker smoke、真实 LLM 端到端、延迟/质量/长稳/播放器基准、`scripts/macos_app_test.sh`（含 UI 自动化）、真实 engine wheel 构建。

2026-09-26 静态门禁收口（用户授权“及时 commit”）：`5dfbd07c` 允许缺少 VoiceDesign 快照时继续解析选择；`87732d6a` 恢复 T02–T11 引入的 40 项 mypy 回归（角色/协议/异步生成器/类型收窄，非 ignore 掩盖）。证据：`uv run --extra dev mypy src` → Success（148 files）；`uv run --extra dev ruff check src tests tools hatch_build.py examples` → All checks passed；受影响的 20 个测试文件 `uv run --extra dev pytest --no-cov -q ...` → **433 tests collected and passed**（2026-09-26 Asia/Shanghai）。T12 仍停在授权/人工验收门；未做部署、切档、真实模型、音频、性能、长稳、播放器或 UI 自动化。

2026-09-26 受控 engine wheel overlay 注入修复：复核发现原 `tools/build_engine_wheel.py` 只把固定上游 checkout 交给 builder，未把 `vendor/mlx-audio-incremental/src` 的增量实现注入构建树；即使 provenance 生成，wheel 也可能不含目标增量引擎。修复为在临时 staged source 中先应用显式 patch、再复制全部 overlay，构建后校验 wheel 成员包含每个 overlay 相对路径，缺失时 fail-closed。TDD：`tests/test_engine_wheel_build.py` 的 fake builder 断言 staged source 含 overlay，并新增缺 overlay archive 拒绝用例。证据：`uv run --extra dev pytest --no-cov -q tests/test_engine_wheel_build.py tests/test_runtime_lock_generation.py tests/test_hatch_build.py` → 22 passed；`uv run --extra dev ruff check src tests tools hatch_build.py examples` → All checks passed；`uv run --extra dev mypy src` → Success（148 files）。

2026-09-26 受控 engine wheel 来源校验与 staging 加固：除 overlay 注入外，builder 现要求 `source_root` 是 `source_revision` 精确检出的 git 工作区，origin 与 `source_repository` 一致，tracked 工作区无改动；staging 只复制该 revision 的 tracked 文件（不打包 `.git`、untracked 文件或 symlink），overlay 若覆盖上游同名文件立即失败；`uv build` 作为默认构建前端，wheel/provenance 在验证轮子成员后才写入目标。TDD 新增 revision mismatch、dirty checkout、untracked 不进入 staging 三组失败回归。证据：`uv run --extra dev pytest --no-cov -q tests/test_engine_wheel_build.py` → 10 passed；组合 `tests/test_engine_wheel_build.py tests/test_runtime_lock_generation.py tests/test_hatch_build.py tests/test_runtime_bootstrap.py tests/test_model_presets.py` → 123 passed；`ruff` / `mypy src tools/build_engine_wheel.py` 通过（2026-09-26 Asia/Shanghai）。

Task 3: complete（供应链真实构建门闭合）— 新增 `vendor/engine-build/engine-build.json`，固定 `Blaizzy/mlx-audio` `v0.5.6` revision `4ab7e6f7dedd69a136cfaa318c5dc8aed5119446`；本地 checkout 只从该 revision 构建，staging 只接收 tracked 上游文件与 tracked incremental overlay，不包含 `.git`/`__pycache__`/untracked。默认 builder 使用 `uv build`，将 `SOURCE_DATE_EPOCH` 固定为上游 commit time；builder 校验 checkout revision/origin/clean、overlay 不覆盖上游、wheel 必有三个 incremental 模块；真实 wheel 连续两次重建 byte-identical。产物：`vendor/engine-build/dist/mlx_audio-0.5.6-py3-none-any.whl` sha256 `a717972afcbbf07dd3ed3ff30f9f26b8c2b20f07664c25c1dae915b0afe57502`；provenance build inputs sha256 `d4e4b9797c2821e8ff25b33f4735daa7eb80e949045162249566c13dde4627cb`，patch/overlay sha256 `a7521bb21ee3d4b22cba13fb4954bb1a24a1fe51e627ff430560f76c9fc34d63`；`runtime-lock.json` 已写同一 `engine_wheel` pin。最终 SpeechRail wheel 门禁验证 lock/provenance/nested engine hash 一致且不再携带旧 overlay 源目录。证据（2026-09-26 Asia/Shanghai）：定向组合 `tests/test_engine_wheel_build.py tests/test_runtime_lock_generation.py tests/test_wheel_contents.py tests/test_model_presets.py tests/test_runtime_bootstrap.py tests/test_hatch_build.py tests/test_installer.py tests/test_service_preflight.py` → 180 passed；`ruff` → All checks passed；`mypy src tools/build_engine_wheel.py` → Success（149 files）。未安装到本机 `com.speechrail` 运行态，未切换 profile/服务；真实模型 wheel 安装与加载仍属 T12。

Task 12: Ruling（用户 2026-09-26 补正，三项）: ①缺失模型一律**异步准备、不阻塞任务**，目录沿用 `models/<artifact_key>`，与其它制品同一套管理（`prepare_spec_models` / `ModelScopeDownloader` / 逐文件 SHA-256 + size 校验），不引入第二套下载或目录口径；②**更高精度制品继承对应低精度制品已通过的门禁证据**，不逐项复测（用户原话「无需测试」），T12 不安排 precision 专项数值门禁测量；③上述行为的下载与实现已获授权（含仓库外模型目录写入）。— cost if wrong: ①若确认要同步阻塞或改目录布局，需回改 `model_store`/`managed_install`/零配置脚本三处；②若日后发现高精度特有缺陷（如 bf16 数值/带宽问题），须补测量并回改能力声明与认证文档；③若下载内容超出 catalog 清单范围，需按仓库外制品清理。
Task 12: Ruling: T12 的「真实测量」降级为**继承证据登记**——文档只写「继承 <低精度档> 门禁证据（未在本机单独实测）」，不得写「已认证」；数值阈值、真实设备/播放器、长稳仍是需用户逐次授权的专项验收。— cost if wrong: 若用户随后要求实测，需重新走 T12 §9 授权门。
Task 12: 范围裁定（零配置脚本漂移）: `.agents/skills/speechrail-zero-setup/scripts/zero_setup.py` 仍调用已删除的 `prepare_diarization_assets(preset_id=…)` 与 `install_managed(preset_id=…)`，属 T02–T11 迁移未覆盖的**真实缺陷**（现有 `tests/test_video_podcast_skill_install.py` 用 fake 打桩旧签名，故未捕获）。本轮按当前契约修复为 `--asr-spec/--tts-spec` + `recommend_selection` + 显式 `--diarization-aligner`（分人为任务 opt-in），并同步 `bootstrap_mac.sh` 用法串与 `SKILL.md`。— cost if wrong: 若零配置路径另有既定改法，需回改该脚本与 skill 文档。
Task 12: Ruling（遗留 preset 基础设施）: `assets/model-catalog.json` 的 `presets`/`precision_policy`、`ModelCatalog.preset()`、`model_store.prepare_models()` 仍是**未被三档选择路径读取的遗留残留**（`ModelCatalog` 目前仍 `Field(min_length=4)` 硬校验四档，删除会牵动 catalog builder、validator 与约 10 个 fixture）。本轮不顺带删除，记为发版后清理项（P5 之外的独立主题）。零配置脚本改用 `recommend_selection` 后，产品路径不再有 legacy preset 消费者。— cost if wrong: 若规格要求「发布即无 legacy preset」，需追加一次 catalog/validator/fixture 重构提交。
