# SDD ledger — plan: docs/superpowers/plans/2026-09-24-model-store-closed-loop.md

Ruling: Execute in the current `main` working tree — the user explicitly said execute; all existing dirty files are disjoint from this plan and will be preserved. No pull/rebase because branch is ahead 6 and behind 1; local source is the current implementation target.
Ruling: The documented `task-start`/`task-done` scripts cannot run because they depend on the absent `subagent-driven-development` skill directory. Use the same TDD gates manually, keep task/test evidence here, and do not fabricate script output.
Pre-flight: Task 1 produces `model_store_root(app_home: Path) -> Path`; Task 2 consumes the canonical `<app_home>/models` destination for verified orphan adoption. Existing `prepare_models` and `inspect_prepared_artifacts` both accept `app_home`; `model_commands` separately computes disk bytes from `<app_home>/models`. A shared resolver can unify these without changing payload interfaces.
Pre-flight: Task 3 retires the exact two Q4 catalog artifacts; Task 4 uses those exact IDs and current local directories only. Existing Q8/BF16 profile mappings remain untouched.

Base: 89aee90dd8f415be48d27369e825cbfd9d01f9a3
Task 1: complete (no commit; branch changes remain uncommitted by request; tests: `uv run --extra dev pytest --no-cov tests/test_model_store.py tests/test_model_commands.py -q` → 58 passed). RED was confirmed by ImportError before adding `model_store_root`; the normal focused command also hit repository `fail_under=80`, so `--no-cov` was used for focused runs.
Task 2: Ruling: a manifest-only cache hit for a registry-owned path can bypass a source-revision change even when fixture bytes happen to be identical — restrict verified-orphan adoption to directories with no registry record at that exact destination; reuse exact registered identities; move stale registered snapshots to `.releases` before downloading, restore them on any transfer/verification failure, and update historical registry paths only on successful publish. Cost: may redownload byte-identical content on revision change, preserving source provenance and upgrade semantics.
Task 2: Ruling: preserve the established cache identity semantics — same source revision, source set, and file manifest is reusable across metadata-only model_id/quantization changes; a source revision or file-manifest change is not. Use `_entry_content_matches_artifact` for ownership-compatible reuse, not the stricter resolver identity helper. The existing metadata-change regression exposed this distinction.
Task 2: Ruling: do not roll back publication twice — inner transaction rollback owns failures after publication starts; the outer handler only restores pre-downloaded backups when failure/cancellation occurs before publication. Double rollback can remove the just-restored destination.

Ruling: Add a dated 2026-09-24 amendment to accepted ADR-0015 — repository search found its current amendment still stated both Q4 artifacts remain in the catalog, contradicting the requested retirement and the active catalog; retain the dated 2026-09-11 history rather than rewrite it. Cost if wrong: expands the planned doc write set by one current decision record; now declared in Task 3.
Task 3: complete — removed only both Q4 artifacts from metadata and generated catalog, removed the ASR Q4 fetch entry, kept all four preset IDs and Q8/BF16 mappings, updated the current acceptance summary and ADR-0015 amendment. RED: catalog assertion failed with 12 expected vs 10 actual after retirement; updated stale count. Tests: `rtk uv run --extra dev pytest --no-cov tests/test_model_catalog_builder.py tests/test_model_commands.py tests/test_model_presets.py -q` → passed.
Task 4: complete — pre-check at `app_home/models` found the exact two Q4 directories as real non-symlink directories and Q4 registry references; active profile was `quality` using Q8. `FileManager.trashItem` moved only the two exact directories to Trash (no Q8/BF16 or `.releases` moved); removed exactly two Q4 registry artifact entries via `_write_registry` atomic update. Post-check: both live Q4 paths absent, registry Q4 references=0, sampled Q8/BF16 directories remain. Before/after model bytes: 27,190,323,243 → 24,783,940,264 (2,406,382,979 bytes reclaimed, based on status deltas).
Task 5: complete — `rtk uv run --extra dev pytest --no-cov tests/test_model_store.py tests/test_model_commands.py tests/test_model_catalog_builder.py tests/test_model_presets.py -q` → passed; `rtk git diff --check` → clean. Source CLI status recognizes the current 10-artifact catalog and Q8 assets; managed installed CLI is older and still lists the retired Q4 keys as `not_downloaded`, while active profile remains `quality`/Q8. No runtime restart, install, release, model load/download, UI automation, commit, or push. Managed runtime catalog will converge on a future authorized source-built release. Existing BF16 integrity-invalid status was present before and after, untouched.
