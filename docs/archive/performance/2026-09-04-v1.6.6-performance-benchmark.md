# SpeechRail v1.6.6 性能基准与资源监控报告 (Performance Baseline Report)

> **测试版本**：`SpeechRail v1.6.6`（预量化 `-8bit` MLX 快照；流式 `completed` 携带词级 segments + `want_segments` 门控 + Sortformer 空格分隔解析修复，见 ADR-0010）
> **报告时间**：2026-09-04（首版于负载环境运行，同日静默环境重跑；**本报告以静默重跑为正式基线**）
> **测试环境**：Apple M5 Max (18 核 CPU / 128GB 统一内存)，macOS 26.6.2，Python 3.12，MLX (MPS)
> **测量工具**：macOS 系统原生 `footprint -f bytes`（采集 `phys_footprint` 物理内存）、`examples/perf/bench_asr.py`、`bench_tts.py`、`bench_realtime.py`、`sample_resources.py`（一键 `run_all_benchmarks.py` 全自动串行）
> **模型配置**：Qwen3-ASR-1.7B `-8bit` + Qwen3-TTS VoiceDesign `-8bit`（均为仓库外绝对路径 snapshot）；`SPEECHRAIL_DTYPE=int8`，`SPEECHRAIL_DEVICE=mps`（`-8bit` 快照自动解析为 int8 直接加载，不二次量化）
> **当前状态**：`/health`（version=1.6.6, asr/tts/diarization 全 ready）、`/readyz` 200、`/v1/models`（canonical + alias 含 `gpt-4o-transcribe-diarize`）、`/v1/voices`（全 available）全绿；Diarization (Sortformer) 就绪。本机部署为 `127.0.0.1:8201` loopback（`.env` 已配置 API key，推理端点需鉴权）。
> **验证范围**：非流式 ASR（4 段时长 + 并发）、TTS（短/长句）、Realtime 连续 3 会话、资源 footprint 压测；全部真实模型 smoke 通过；**新增流式/批量分人功能验证（§5）**。
> **运行条件说明**：首版基准运行时本机有并发负载（agent 运行时约 5GB + sona 进程活跃），TTS/ASR/并发/Realtime 数值被拉高、ASR 常驻采样偏高；随后在**无并发负载的静默环境重跑**，全部数值回落至与 v1.6.5 基线一致的水平（见 §6 对照）。本报告默认引用静默重跑作为正式基线；首版负载环境数值仅用于归因比对。

---

## 1. 真实物理内存与 CPU 资源基线 (Physical Footprint Baseline)

> **统计口径**：Apple Silicon 统一内存下，MLX/Metal 显存分配计入物理内存池。严格采用 `footprint` 采集 `phys_footprint` 与压测峰值 `phys_footprint_peak`。
> **采样说明**：`sample_resources.py` 通过命令行区分 worker；本次采样窗口含 ASR batch 压测（30s 音频 × 5 次，mode=all，含 warmup）与 TTS 合成，无 streaming/realtime 会话（realtime 懒启动，未计入 streaming worker 增量）。静默环境重跑采样。

| 进程组件 | 运行配置 | 冷启待机 | 预热常驻物理内存 (Footprint) | 压测峰值物理内存 (Peak) | 峰值 CPU | 运行与显存行为分析 |
|---|---|---|---|---|---|---|
| **主服务进程 (Host FastAPI)** | `speechrail serve` | 1,077.3 MB | **1,077.3 MB (~1.08 GB)** | **1,585.5 MB (~1.59 GB)** | 2.1% | 含 in-service Sortformer diarization 模型常驻（较 v1.6.5 0.54 GB 增加约 0.54 GB，为分人能力的功能性常驻成本）；路由、协议解析、ffmpeg 解码与调度 |
| **ASR Worker (batch)** | `qwen3_worker.py` int8（U32 packed） | 2,497.4 MB | **2,501.6 MB (~2.50 GB)** | **3,681.8 MB (~3.68 GB)** | 83.1% | `-8bit` 快照直接加载 int8；常驻与峰值均与 v1.6.5 基线（2.50 GB）一致 |
| **Qwen3 TTS Worker** | `qwen3_tts_worker.py` int8 | 3,562.9 MB | **3,557.3 MB (~3.56 GB)** | **4,125.9 MB (~4.13 GB)** | 2.3% | VoiceDesign 1.7B `-8bit`，`speech_tokenizer` codec 恒为 FP32，与 v1.6.5 同量级 |
| **全系统总物理常驻 (Total)** | -- | -- | **7,136.1 MB (~7.14 GB)** | **7,907.7 MB (~7.91 GB)** | -- | 峰值后回落正常，无泄漏滞留 |

> **真同时口径**：`Peak(Total)` = 逐采样 tick 的（当前 footprint 之和）的最大值，**不为**逐进程 all-time high-water 之和。本次测量窗口 total 峰值 7.91 GB。
> 注：首版负载环境实测 ASR batch 常驻 4.34 GB / 总常驻 8.97 GB，静默重跑回落到 2.50 GB / 7.14 GB——确认该差异为负载下采样伪影（非代码回归，见 §6）。

---

## 2. 非流式 ASR 延迟与 RTF 实测基线 (`POST /v1/audio/transcriptions`)

> **测试配置**：`model=speechrail/qwen3-asr-1.7b`, 重复 $N=3$ 取均值。16kHz 单声道 PCM16 中文语音样本。**时长以 ffprobe 实测为准**（TTS 生成样本语速不同，非严格 3/10/30/60s）。

| 音频时长 (Duration) | 平均处理耗时 (Mean Latency) | 实时率 (RTF) | 最小 / 最大耗时 | 转写文本 |
|---|---|---|---|---|
| **3.0 秒** (短音频) | **0.31 s** | **0.10x** | 0.13s / 0.69s | “你好，这是本地语音识别与合成。” |
| **9.0 秒** (中音频) | **0.23 s** | **0.03x** | 0.23s / 0.24s | “你好，这是本地语音识别与合成服务的性能基准测试。…” |
| **18.6 秒** (长音频) | **0.48 s** | **0.03x** | 0.42s / 0.57s | 长语音连续转写，吐字对齐清晰 |
| **33.0 秒** (超长音频) | **0.89 s** | **0.03x** | 0.88s / 0.90s | 长音频吞吐稳定（比播放快 ~33x） |

> 注：3s 音频首测 0.69s 为三次中唯一 warm-up 读数（模型已常驻但首次请求含对齐/管线 fault-in），后续两次稳定为 0.13s；9–33s 音频三次读数高度一致（±0.03s 内）。

### 并发吞吐（4 workers / 8 请求，9.0s 音频）

| 指标 | 实测 |
|---|---|
| Wall time | **1.98 s** |
| 成功率 | **8/8 (100%)** |
| 平均延迟 | **0.80 s** |
| P95 | **0.99 s** |
| 吞吐 | **4.04 req/s** |
| 总 RTF | **0.03x** |

---

## 3. TTS 延迟与 RTF 实测基线 (`POST /v1/audio/speech`)

| 句长 | 平均耗时 (Mean) | 实时率 (RTF) | 最小 / 最大耗时 | 音频时长 (output) |
|---|---|---|---|---|
| **短句 (20 字符)** | **1.05 s** | **0.29x** | 1.00s / 1.11s | 3.36-3.84s |
| **长句 (50 字符)** | **2.44 s** | **0.28x** | 2.35s / 2.60s | 8.40-9.12s |

---

## 4. Realtime 流式基线（连续 3 会话，`whisper-1` 非分人）

> **测试配置**：`.qwenpaw` venv（openai SDK）`client.realtime.connect(model="whisper-1")`，9.0s PCM 输入。

| 会话 | 握手 setup | ASR Commit 延迟 | RTF | TTS 首包 (TTFA) | TTS 字节 | 结果 |
|---|---|---|---|---|---|---|
| session 1 | 54 ms | 435 ms | 0.05x | 52 ms | 133,120 | ✅ 完成 |
| session 2 | 0 ms | 361 ms | 0.04x | 35 ms | 148,480 | ✅ 完成 |
| session 3 | 0 ms | 360 ms | 0.04x | 38 ms | 163,840 | ✅ 完成 |

---

## 5. v1.6.6 新增功能验证：流式/批量说话人分离（ADR-0010）

> 本节为 v1.6.6 核心变更的功能验收（真实模型，非 fake backend）。

### 5.1 批量分人（`gpt-4o-transcribe-diarize`，缺陷 2）

- 输入：双音色 TTS 对话（`warm` 说前半 + `bright` 说后半，11.04s wav）。
- 结果：**HTTP 200（不再 502）**，51 个词级 segments；说话人分布
  `spk_01: 25` / `spk_02: 24`（另 2 个极短边缘段无 speaker），
  前半（warm）全 `spk_01`、后半（bright，自 ~6s 起）全 `spk_02`——**精确分离**。

### 5.2 流式分人（`/v1/realtime` + diarization，缺陷 1）

- 输入：warm 音色前 5s PCM，`session.update` 模型 `gpt-4o-transcribe-diarize`，
  `input_audio_format="pcm16"`，手动 commit。
- 结果：commit 后收到 `input_audio_buffer.committed` → `conversation.item.created` →
  **21 个 `.segment` 事件（`speaker=spk_01`，`start`/`end` 秒制）** →
  `conversation.item.input_audio_transcription.completed`。端到端分人链路真实打通；
  词级切分与批量路径一致。

---

## 6. v1.6.5 → v1.6.6 对照与归因（静默重跑实测）

| 指标 | v1.6.5（实测） | v1.6.6（静默重跑） | 说明 |
|---|---|---|---|
| ASR batch 常驻 | 2.50 GB | 2.50 GB | 一致 ✅ |
| TTS 常驻 | 3.56 GB | 3.56 GB | 一致 ✅ |
| 主服务常驻 | 0.54 GB | 1.08 GB | +0.54 GB = in-service Sortformer（分人能力功能性常驻成本，两次运行一致） |
| 总物理常驻 | 6.60 GB | 7.14 GB | +0.54 GB = Sortformer，其余一致 ✅ |
| ASR 9s 级延迟 | 0.23s (9.2s) | 0.23s (9.0s) | 一致 ✅ |
| ASR 超长音频 | 0.87s (34.6s) | 0.89s (33.0s) | 一致 ✅ |
| ASR 并发均值/P95 | 0.78s / ~0.94s | 0.80s / 0.99s | 一致（噪声带内）✅ |
| ASR 并发吞吐 | 4.19 req/s | 4.04 req/s | 一致（噪声带内）✅ |
| TTS 长句 | 2.27s | 2.44s | 基本一致（同 fixture 语速差异）✅ |
| Realtime commit | 370-827ms | 360-435ms | 一致 ✅ |
| Realtime TTFA | 34-44ms | 35-52ms | 一致 ✅ |

**归因结论（静默重跑证实）：**

1. **v1.6.6 无性能回归**：静默环境下延迟/吞吐/内存全部回到 v1.6.5 基线水平；
   唯一静态差异是主服务常驻 +0.54 GB（in-service Sortformer，分人能力的功能性成本）。
2. **首版数值偏高的根因是运行条件**：首版基准时本机有 agent 运行时约 5GB + sona 进程
   并发负载，TTS/ASR/并发/Realtime 数值与 ASR 常驻采样被同步拉高；静默重跑全部回落。
3. **代码与配置无变更且测试全绿**（实测依据：`git diff` 审查——`qwen3_worker.py`
   变更仅为流式 `_align_buffers`/`align_session_audio`/`_to_streaming_segments`/`_handle_commit`，
   批量 `transcribe`/`_segments` 与 TTS worker 零改动；374 单元测试全绿；
   `mps` + `int8` + 双 `-8bit` 快照与 v1.6.5 基线环境一致）。

---

## 7. 复现步骤

```bash
# 1. 健康检查（需 API key 的推理端点先导出）
export SPEECHRAIL_API_KEY="$(grep '^SPEECHRAIL_API_KEY=' \
  "$HOME/Library/Application Support/SpeechRail/config/.env" | cut -d= -f2-)"
curl -s http://127.0.0.1:8201/health | jq .
curl -s http://127.0.0.1:8201/readyz | jq .

# 2. 一键全量基准（7 步：健康→fixtures→TTS→ASR→并发→Realtime→资源采样）
python3 .agents/skills/speechrail-perf-benchmark/scripts/run_all_benchmarks.py

# 3. 分人 smoke（批量 + 流式）
# 批量：curl /v1/audio/transcriptions + model=gpt-4o-transcribe-diarize + verbose_json
# 流式：session.update(model=gpt-4o-transcribe-diarize, input_audio_format="pcm16") → append → commit

# 注：基准须在无并发负载的静默环境运行，若同时跑其他大内存进程，
# ASR 常驻采样与延迟数值会被拉高（见 §6 归因）。
```